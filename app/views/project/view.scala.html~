@(project: models.Project)

@main(":: LOGIT ::") {
<h2>@project.name</h2>
<fieldset>
  <legend>Log entries</legend>
<table class="logs">
   @project.logs.map{ log =>
      <tr>
      @log.dataMap.map { data =>
         <td>@data._1</td>
      }
      </tr>
      <tr>
      @log.dataMap.map { data =>
         <td>@data._2</td>
      }
      </tr>
      <br/>
   }
</table>
</fieldset>

<script type="text/javascript">
  var project = new Project("@project.name");
  window.setTimeout(project.refresh, 2000);
</script>
}
